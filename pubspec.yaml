# pubspec.yaml (contenido completo)

name: retos_diarios_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  http: ^1.2.1
  shared_preferences: ^2.2.3
  confetti: ^0.7.0
  flutter_svg: ^2.0.10+1
  google_generative_ai: ^0.4.7
  flutter_background_service: ^5.1.0
  flutter_local_notifications: ^19.3.0
  permission_handler: ^12.0.1
  supabase_flutter: ^2.9.1
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # # Para agregar assets a tu aplicación, agrega una sección de assets como esta:
  assets:
    - assets/
